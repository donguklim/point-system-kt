# point-system-kt
A point system implementation using Kotlin


## Running the project
```shell
docker-compose build
docker-compose up
```

## Running the test
```shell
docker-compose run --rm test
```
